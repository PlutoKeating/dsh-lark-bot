## Highlights / 本版亮点

- **AI 检索可发现性重构**：README 改为「场景 → 亮点 → 30 秒上手 → 完整使用 → FAQ」的用户向头部结构，自动化 / 合规章节整体移后并完整保留；关键词章节与 GitHub Topics 对齐（新增 `im-bridge` / `ai-agent` / `workspace` / `self-healing`）。
- **npm 搜索元数据刷新**：keywords 补齐中英场景词（`feishu-plugin` / `lark-integration` / `remote-coding` 等），描述保持中英双语，提升 npm 搜索与 AI 检索命中。
- **落地页 FAQ 同步**：FAQPage 结构化数据补充「多项目 / 多人协作」典型用例。

**安装 / 升级 · Install & upgrade**：

```bash
npx dsh-lark-bot@latest setup --profile dsh-lark
npx dsh-lark-bot@latest upgrade --profile dsh-lark --yes
```

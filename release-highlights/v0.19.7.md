# v0.19.7 — Reliable cards, terminal errors, and vision input

- Fixes #106: process-card re-anchoring is serialized with in-flight patches, preventing a recalled card from producing a stray fallback notice or receiving updates after replacement.
- Fixes #107: ordinary SDK resume errors now reach the run-state reducer and terminalize the process card instead of leaving it permanently in “thinking” with an active Stop button.
- Completes #105/#96: selected DeepSeek vision models are persisted into the runtime catalog with `inputModalities: [text, image]` before the agent run, so rc.8 accepts real image blocks.
- Preserves deployment YAML comments and unrelated model fields through AST-targeted runtime catalog updates, with real bridge-engine wiring and idempotence regression coverage.
- Verified against the managed dsh rc.8 SDK image path, full local test suite, TUI admission/PTY boundaries, and the publish bundle gate.

Upgrade and reload the managed profile:

```bash
npx dsh-lark-bot@latest upgrade --profile dsh-lark --yes --restart
```

# v0.18.0 — Channel-native self-configuration and reliability

- Makes every SDK, ACP, and Web turn explicitly aware of its dsh-lark-bot Feishu/Lark channel, and registers a lifecycle-bound official runtime Skill for install, configuration, diagnostics, commands, and deployment guidance (#85).
- Adds owner-only secure secret forms plus `lark_request_secret`; API keys and App Secrets bypass prompts, sessions, jobs, archives, logs, diagnostics, and cloud LLM requests (#85).
- Enforces scope permission policy before fast paths and plan gates, including deny precedence and anti-circumvention behavior (#82, #84).
- Resolves provider/model routes from effective dsh configuration before SDK and ACP startup and preserves managed service environment values across restarts (#83).
- Revalidates the reporter's session-isolation reproduction and keeps runtime reuse scoped by both conversation and workspace (#79).
- Adds a daily, least-privilege dsh and dsh-TUI release watcher with GitHub/npm deduplication and safe upstream-note handling (#54).

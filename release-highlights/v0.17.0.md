# v0.17.0 — Community reliability round

- Keeps every process-card renderer below the Feishu transport budget while preserving the newest safe tool statuses and keeping raw payloads private.
- Hardens plan approval with one-call grants, fail-closed shell arguments, configurable gating, audit-safe redaction, and truthful delivery failures.
- Makes long-poll callbacks, card patch retries, and native-session recovery resilient to timeouts, disconnects, and stale session bindings.
- Fixes macOS path and PTY compatibility, validates `/cd` targets, clarifies transient model and `/effort` behavior, and adds configurable plain-reply language.
- Removes the misleading guardian query-process PID and fixes the packaged Cordis concurrency expression.

## Highlights / 本版亮点

- **修复飞书交互卡片回调**：Card JSON 2.0 按钮统一使用官方 `behaviors.callback` 协议；
  新扫码注册显式申请 `card.action.trigger`，避免消息收发正常但按钮点击完全不送达。
- **审批反馈不再静默**：计划、逐工具审批和问答点击都会写入结构化入站日志；过期卡片返回
  明确的双语错误 toast，成功提交继续使用原生 toast、终态确认和原卡撤回。
- **长时间人工决策可靠性**：本地 `/ask`、`/plan`、`/approval` 回调以合法 JSON 空白流保活，
  避免 Node/Undici 在等待五分钟后以 `fetch failed` 中断仍有效的任务。

**升级 · Upgrade**：

```bash
npx dsh-lark-bot@latest upgrade --profile dsh-lark --yes --restart
```
